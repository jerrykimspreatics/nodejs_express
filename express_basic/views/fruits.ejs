<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>템플릿 ejs</title>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <style>
        img{
            width: 300px;
            height: 230px;
            border-radius: 10px;
        }
    </style>
</head>
<body>
    <h2>이미지 바꾸기</h2>
    <div>
        <img src="images/question.jpg" alt="물음표">
    </div>
    <% for(let i=0; i<fruits.length; i++){ %>
        <button class="btn <%=fruits[i].name %>"><%= fruits[i].kor %></button>
    <% } %>

    <script>
        $('.btn').click(function () {
            let name; //파일명
            if ($(this).hasClass('watermelon')) {
                name = 'watermelon';
            } else if ($(this).hasClass('grape')) {
                name = 'grape';
            } else if ($(this).hasClass('strawberry')) {
                name = 'strawberry';
            }
            $('img').attr('src', `images/${name}.jpg`);
        });
    </script>
</body>
</html>