<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>템플릿 ejs</title>
</head>
<body>
    <h2><%= dan %>단</h2>
    <% for(let i=1; i<10; i++) { %>
        <div>
            <%= `${dan} x ${i} = ${dan*i}` %>
        </div>
    <% } %>
    
    <h2>전체 구구단</h2>
    <% for(let i=2; i<10; i++) {
        for(let j=1; j<10; j++){ %>
        <div>
            <%= `${i} x ${j} = ${i*j}` %>
        </div>
        <% } %><br>
    <% } %>
</body>
</html>